export interface RolesType {
    MANAGER: string,
    EMPLOYEE_STALL: string,
    EMPLOYEE_REST: string
}

export const ROLES = {
    MANAGER: 'manager',
    EMPLOYEE_STALL: 'employee-stall',
    EMPLOYEE_REST: 'employee-restaurant' 
}